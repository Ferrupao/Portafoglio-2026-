name: Build Portafoglio 2026 V3.1

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/android-build-v31.yml"
      - "portafoglio-v31.zip"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 35
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Extract V3.1 project
        run: |
          rm -rf android-project-v31
          mkdir android-project-v31
          unzip -q portafoglio-v31.zip -d android-project-v31

      - name: Build V3.1 APK
        working-directory: android-project-v31
        run: gradle assembleDebug --no-daemon

      - name: Upload V3.1 APK
        uses: actions/upload-artifact@v4
        with:
          name: Portafoglio2026-V31-APK
          path: android-project-v31/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
